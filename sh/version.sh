#!/bin/bash

echo 'node version'
nodejs --version
echo 'npm version'
npm --version
echo 'nvm version'
nvm --version

echo 'mongo version'
mongo --version
echo 'mongod version'
mongod --version

echo 'docker version'
docker --version
echo 'docker-compose version'
docker-compose --version
